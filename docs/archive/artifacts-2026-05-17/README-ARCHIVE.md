# Archived Artifacts

This folder contains non-active artifacts moved out of runtime/source paths during cleanup.

- `public-central-app.js.corrupt` came from `public-central/app.js.corrupt`.

Do not use these files as source of truth.
